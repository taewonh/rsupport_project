

server:
  servlet:
    context-path: /rsupport

spring:
  datasource:
    url: jdbc:h2:~/testDB
    driverClassName: org.h2.Driver
    username: sa

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update